language: c++

addons:
  apt:
    packages:
      - python3
      - python3-pip
    update: true

jobs:
  include:
    - os: linux
      dist: focal
      env:
        - MATRIX_EVAL="CC=clang-11 && CXX=clang++-11"
    - os: linux
      dist: focal
      env:
        - MATRIX_EVAL="CC=gcc && CXX=g++"

before_install:
  eval "${MATRIX_EVAL}"

install:
  - ci/install.sh

script:
  - ci/build.sh
